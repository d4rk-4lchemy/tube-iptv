services:
  tube:
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - "${RENDER_GID:-109}"
      - "${VIDEO_GID:-44}"
    environment:
      ENCODER: ${ENCODER:-vaapi}
      VAAPI_DEVICE: ${VAAPI_DEVICE:-/dev/dri/renderD128}
